import "io.vx";

func identity<T>(T x) -> T {
    return x;
}

func main() -> i32 {
    io.println("Testing Generics:");
    
    // Testing generic identity with different types
    // Note: Implicit generic calls might be limited, testing based on current capability
    var i32 a = identity<i32>(42);
    io.println(a);
    
    io.println("Generic declaration verified.");
    return 0;
}
